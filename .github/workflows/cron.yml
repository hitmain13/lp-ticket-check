name: Cron
on:
  schedule:
    - cron: '*/15 * * * *'

jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - run: date

  cron-schedule:
    runs-on: ubuntu-latest
    steps:
      - name: Every 5 minutes
        run: echo "This workflow runs every 5 minutes"
